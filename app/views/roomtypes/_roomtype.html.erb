<div class="jumbotron">
  <h1 class="display-4"><%= roomtype.name %></h1>
  <p class="lead"><%= roomtype.code %> | <%= roomtype.name %> | <%= roomtype.max_guest %> | <%= roomtype.cancel_charges %> </p>
  <div>
  <p><%= link_to "Edit", edit_roomtype_path(@roomtype) , class:"btn btn-secondary"%> |
  <%= link_to "Back", roomtypes_path , class:"btn btn-dark"%> | <%= button_to "Delete", @roomtype, method: :delete, class:"btn btn-outline-danger sm" %></p>
</div>
</div>

